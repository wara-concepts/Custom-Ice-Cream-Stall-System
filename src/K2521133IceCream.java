// The Decorator component. Both the plain ice cream and every wrapper that adds
// a topping or sauce answer these two questions, so the two can be stacked.
public interface K2521133IceCream {
    String getDescription();

    double getCost();
}
